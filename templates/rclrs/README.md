# rclrs template

This is a minimal Rust template using rclrs.
- The template pins `rclrs` to `ros2_rust` tag `v0.6.0` in `Cargo.toml`.
- Rename the crate and node name as needed.
- Common helpers live in `src/qos.rs`, `src/topics.rs`, and `src/services.rs`.
