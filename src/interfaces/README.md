# lesson_interfaces

Shared message/service/action definitions for lessons.

Add new interface files under:
- `msg/`
- `srv/`
- `action/`

Then update `CMakeLists.txt` to include them in `rosidl_generate_interfaces()`.
